return
{
  "akinsho/toggleterm.nvim",
  version = "*",
  config = true,
  opts = {
    size = 15,
    open_mapping = [[<C-CR>]],
    direction = "horizontal", -- or "float", "vertical"
    close_on_exit = false,
  },
}
